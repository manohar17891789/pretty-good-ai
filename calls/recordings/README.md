Downloaded call recordings (`<scenario>_<timestamp>.mp3`) land here
automatically via `src/recording.py` after each call completes. None
exist yet in this checkout — see `calls/transcripts/README.md` for why.
Run `python -m src.run_all` after filling in `.env` to populate this
folder with at least 10 recordings.
